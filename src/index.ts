import { ContentOpfInput, ContentOpfSection, ExpInput, ExpParagraph, Image, IndexContent, IndexHeader, IndexInput, IndexSection, InfoInput, NavChapter, NavInput, NavSection, SectionBody, SectionInput, TocInput, TocSection, createContentOpf, createExp, createIndex, createInfo, createNav, createSection, createToc, setup } from './resource.js';
export { createContentOpf, createExp, createIndex, createInfo, createNav, createSection, createToc, setup };
export type { ContentOpfInput, ContentOpfSection, ExpInput, ExpParagraph, Image, IndexContent, IndexHeader, IndexInput, IndexSection, InfoInput, NavChapter, NavInput, NavSection, SectionBody, SectionInput, TocInput, TocSection };

